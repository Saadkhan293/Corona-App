import React from 'react';
import axios from 'axios';
import { StyleSheet, Text, View, navigation, Button, FlatList, ActivityIndicator } from 'react-native';
import { useState, useEffect } from 'react';
function WorldStatistics({ navigation }) {
    return(
        <View>
            
        </View>
    )
   

}
const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
});

export default WorldStatistics;
